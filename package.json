{
  "type": "module",
  "name": "weatherwise",
  "version": "1.0.0",
  "description": "Accurate, plain-English weather forecast for Indian cities",
  "main": "index.js",
  "scripts": {
  "fetch": "node --env-file=.env fetch-weather.js",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "keywords": [],
  "dependencies": {
    "@google/genai": "^2.15.0",
    "dotenv": "^17.4.2"
  }
}
